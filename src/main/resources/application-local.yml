# Local dev profile — mirrors docker-compose.yml credentials (dev-only; ECS injects env vars that
# take precedence and never activates this profile). Run: SPRING_PROFILES_ACTIVE=local ./mvnw spring-boot:run
spring:
  datasource:
    password: postgres

keycloak:
  admin:
    password: admin

minio:
  access-key: minioadmin
  secret-key: minioadmin
