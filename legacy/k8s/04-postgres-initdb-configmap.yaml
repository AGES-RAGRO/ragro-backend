apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-initdb
  namespace: ragro
data:
  00-create-keycloak-db.sh: |
    #!/bin/bash
    set -e
    psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" <<-EOSQL
        CREATE DATABASE keycloak;
    EOSQL
